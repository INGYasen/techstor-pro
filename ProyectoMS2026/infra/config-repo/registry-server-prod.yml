server:
  port: ${PORT:7081}

spring:
  application:
    name: registry-server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  endpoints:
    web:
      exposure:
        include: "*"